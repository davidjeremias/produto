server:
  port: 8082
  servlet:
    context-path: /produto-backend
  
spring:
  application:
    name: produto
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/u2d-produto?allowPublicKeyRetrieval=true&sslMode=DISABLED&createDatabaseIfNotExiste=true
    username: root
    password: 415782
  flyway:
    baseline-on-migrate: true
  jmx:
    enabled: false
  
